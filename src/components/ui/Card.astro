---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`bg-white rounded-lg shadow-md p-6 transition-shadow hover:shadow-lg ${className}`}>
  <slot />
</div>
